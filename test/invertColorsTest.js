'use strict';
const expect = require('chai').expect;
const fs = require('fs');
const readBitmapHeaders = require(__dirname + '/../lib/readBitmapHeaders.js');

describe('invertColorsTest:', function(){
  it('should invert the colors of an RGBA unit', function(){
    throw new Error('invertColorsTest fail');
    
  });
});
